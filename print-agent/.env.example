# The Kulode API base URL (note: /api/v1, not the app URL)
KULODE_API_URL=https://api.kulode.com/api/v1

# Generate this from Settings > Printers > Print Agent in Kulode
KULODE_PRINT_AGENT_TOKEN=

# Optional tuning (defaults shown)
# KULODE_POLL_INTERVAL_MS=2000
# KULODE_PRINT_TIMEOUT_MS=5000
